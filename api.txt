p = Prophet.new do
	bot 'localhost:2099' do
		on :announce do
			msg.upcase
		end
	end
	
	#not implemented yet
	jabber 'localhost:2093' do
		on :announce do
			erb msg, :newmsg
		end
	end
end

p.up! # all endpoints rise!

p.announce 'hello' # say something over endpoints.